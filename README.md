# Daily Experiments

Automated security scanner that pulls recent CVEs from the NIST National Vulnerability Database, parses CVSS scores, identifies top weakness categories (CWEs), and generates daily threat reports.

Runs daily via GitHub Actions. Reports in `logs/`.
